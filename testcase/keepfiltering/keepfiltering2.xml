<?xml version="1.0" encoding="UTF-8"?>
<project name="keepfiltering2_xhtml" default="keepfiltering22xhtml" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <target name="keepfiltering22xhtml">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${dita.dir}${file.separator}testdata${file.separator}keepfiltering${file.separator}sequence.ditamap"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}keepfiltering1${file.separator}xhtml1"/>
      <property name="transtype" value="xhtml"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}keepfiltering1${file.separator}xhtml1"/>
      <property name="dita.input.valfile"
                location="${dita.dir}${file.separator}testdata${file.separator}keepfiltering${file.separator}garage_filtering.ditaval"/>
    </ant>
  </target>
</project>