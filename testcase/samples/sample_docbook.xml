<?xml version="1.0" encoding="UTF-8"?>
<project name="sample_docbook" default="sample2docbook" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  <target name="sample2docbook">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${testdata.dir}${file.separator}samples${file.separator}sequence.ditamap"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}samples${file.separator}sequence${file.separator}docbook"/>
      <property name="transtype" value="docbook"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}samples${file.separator}sequence${file.separator}docbook"/>
    </ant>
  </target>
</project>