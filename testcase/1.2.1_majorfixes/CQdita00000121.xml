<?xml version="1.0" encoding="UTF-8" ?>
<project name="CQdita0000121" default="cq00001212wordrtf" basedir=".">
    <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
    <property name="dita.temp.dir" value="${dita.dir}${file.separator}temp"/>
    <import file="${dita.dir}${file.separator}build.xml"/>
    <target name="cq00001212wordrtf">
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input"
                value="${dita.dir}${file.separator}testdata${file.separator}Table${file.separator}property.dita"/>
            <property name="output.dir"
                value="${dita.dir}${file.separator}testresult${file.separator}Table${file.separator}property${file.separator}wordrtf"/>
            <property name="transtype" value="wordrtf"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}temp${file.separator}Table${file.separator}property${file.separator}wordrtf"/>
            
        </ant>   
    </target>
    
</project>

