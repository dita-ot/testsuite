<?xml version="1.0" encoding="UTF-8"?>
<project name="03_pdf" default="032pdf" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  <target name="032pdf">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${testdata.dir}${file.separator}03${file.separator}03.ditamap"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}03${file.separator}pdf"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}03${file.separator}pdf"/>
      <property name="transtype" value="pdf"/>
    </ant>
  </target>
</project>