<?xml version="1.0" encoding="UTF-8" ?>

<project name="19_TC4_javahelp" default="19_TC42javahelp" basedir=".">
  
  <!-- dita.dir should point to the toolkit's root directory -->
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  
  <import file="${dita.dir}${file.separator}integrator.xml"/>
  
  <target name="19_TC42javahelp" depends="integrate">
    <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
      <!-- please refer to the toolkit's document for supported parameters, and 
           specify them base on your needs -->
      <property name="args.input"
        value="${testdata.dir}${file.separator}19${file.separator}TC4.ditamap"/>
      <property name="dita.temp.dir" value="${dita.dir}${file.separator}temp${file.separator}19${file.separator}TC4${file.separator}javahelp"/>
      <property name="output.dir"
        value="${dita.dir}${file.separator}testresult${file.separator}19${file.separator}TC4${file.separator}javahelp"/>
      <property name="transtype" value="javahelp"/>
    </ant>
  </target>
</project>
