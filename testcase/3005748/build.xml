<?xml version="1.0" encoding="UTF-8"?>
<project name="3005748" default="all" basedir=".">
   <dirname property="test.basedir" file="${ant.file.3005748}"/>
   <property name="test.name" value="3005748"/>
   <property name="currentdir" location="${test.basedir}"/>
   <property name="temp.dir" location="${dita.dir}/temp/${test.name}"/>
   <property name="result.dir" location="${dita.dir}/testresult/${test.name}"/>
   <target name="all">
      <echo message="${currentdir}" level="info"/>
      <sequential>
         <subant inheritall="true">
            <fileset dir="${currentdir}" includes="3005748*.xml"/>
            <property name="test.name" value="3005748"/>
         </subant>
      </sequential>
   </target>
</project>