<?xml version="1.0" encoding="utf-8"?><project name="chunk" default="chunk" basedir=".">
    <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
    
    <target name="chunk">
        <!-- chunk_duplicate_tocontent -->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}chunk_duplicate_tocontent${file.separator}chunk_duplicate.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_duplicate_tocontent${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_duplicate_tocontent${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant> 
        <!-- chunk_rewrite_tocontent /chunk_rewrite_topicID.ditamap-->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}chunk_rewrite_topicID.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}chunk_rewrite_topicID${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}chunk_rewrite_topicID${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant> 
        
        <!-- chunk_rewrite_tocontent /topicgroup_chunk.ditamap-->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}topicgroup_chunk.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}topicgroup_chunk${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_rewrite_tocontent${file.separator}topicgroup_chunk${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant> 
        
     <!-- unware_chunk_content -->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}unware_chunk_content${file.separator}maplink.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}unware_chunk_content${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}unware_chunk_content${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant> 
        
        <!--chunk_map_tocontent -->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}chunk_map_tocontent${file.separator}map.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_map_tocontent${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_map_tocontent${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant>
        
        <!-- chunk_hogs_memory -->
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input" value="${dita.dir}${file.separator}testdata${file.separator}chunk${file.separator}chunk_hogs_memory${file.separator}map.ditamap"/>
            <property name="output.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_hogs_memory${file.separator}xhtml"/>
            <property name="transtype" value="xhtml"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}chunk_hogs_memory${file.separator}xhtml${file.separator}temp"/>
            <property name="args.logdir" value="${dita.dir}${file.separator}testresult${file.separator}chunk${file.separator}log"/>
            <property name="clean.temp" value="no"/>
        </ant>
    </target>
</project>