<?xml version="1.0" encoding="UTF-8"?>
<project name="unknowforeign_generalize" default="generalize" basedir=".">
  <property name="dita.dir" location="${basedir}/../.."/>
  <property name="dita.temp.dir" location="${dita.dir}/temp"/>
  <target name="generalize">
    <xslt basedir="${dita.dir}/testdata/unknownforeign/generalize"
          destdir="${dita.dir}/testresult/unknownforeign/generalize"
          style="${dita.dir}/xsl/generalize.xsl"
          extension=".xml"/>
  </target>
</project>