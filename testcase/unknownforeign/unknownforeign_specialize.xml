<?xml version="1.0" encoding="UTF-8" ?>

<project name="unknowforeign_specialize" default="specialize" basedir=".">
    
    <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
    <property name="dita.temp.dir" value="${dita.dir}${file.separator}temp"/>
    
    <target name="specialize">
        <xslt basedir="${dita.dir}${file.separator}testdata${file.separator}unknownforeign${file.separator}specialize" destdir="${dita.dir}${file.separator}testresult${file.separator}unknownforeign${file.separator}specialize"
            style="${dita.dir}${file.separator}xsl${file.separator}specialize.xsl" extension=".xml"/>
    </target>
</project>