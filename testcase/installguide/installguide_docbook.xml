<?xml version="1.0" encoding="UTF-8"?>
<project name="installguide_docbook" default="installguide2docbook" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  <target name="installguide2docbook">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${testdata.dir}${file.separator}installguide${file.separator}installing-ditaot.ditamap"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}installguide${file.separator}docbook"/>
      <property name="transtype" value="docbook"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}installguide${file.separator}docbook"/>
    </ant>
  </target>
</project>