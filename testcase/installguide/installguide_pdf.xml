<?xml version="1.0" encoding="UTF-8"?>
<project name="installguide_pdf" default="installguide2pdf" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  <target name="installguide2pdf">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${testdata.dir}${file.separator}installguide${file.separator}installing-ditaot.ditamap"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}installguide${file.separator}pdf"/>
      <property name="transtype" value="pdf"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}installguide${file.separator}pdf"/>
    </ant>
  </target>
</project>