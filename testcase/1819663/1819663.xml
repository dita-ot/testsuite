<?xml version="1.0" encoding="UTF-8" ?>
<project name="1819663_eclipsehelp" default="18196632eclipsehelp" basedir=".">
    <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
   
    
    <target name="18196632eclipsehelp">
        <ant antfile="${dita.dir}${file.separator}build.xml" target="init">
            <property name="args.input"
                value="${dita.dir}${file.separator}testdata${file.separator}1819663${file.separator}1819663.dita"/>
            <property name="output.dir"
                value="${dita.dir}${file.separator}testresult${file.separator}1819663${file.separator}eclipsehelp"/>
            <property name="transtype" value="eclipsehelp"/>
            <property name="dita.temp.dir" value="${dita.dir}${file.separator}temp${file.separator}1819663${file.separator}eclipsehelp"/>
            
        </ant>   
    </target>
</project>

