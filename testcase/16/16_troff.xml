<?xml version="1.0" encoding="UTF-8"?>
<project name="16_troff" default="162troff" basedir=".">
  <property name="dita.dir" value="${basedir}${file.separator}..${file.separator}.."/>
  <property name="testdata.dir" value="${dita.dir}${file.separator}testdata"/>
  <target name="162troff">
    <ant antfile="${dita.dir}${file.separator}build.xml">
      <property name="args.input"
                location="${testdata.dir}${file.separator}16${file.separator}16.ditamap"/>
      <property name="dita.temp.dir"
                location="${dita.dir}${file.separator}temp${file.separator}16${file.separator}troff"/>
      <property name="output.dir"
                location="${dita.dir}${file.separator}testresult${file.separator}16${file.separator}troff"/>
      <property name="transtype" value="troff"/>
    </ant>
  </target>
</project>